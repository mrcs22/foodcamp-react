export default function Footer() {
  return (
    <div class="footer">
      <button id="finishSelectionButton" disabled>
        Selecione os 3 itens para fechar o pedido
      </button>
    </div>
  );
}
